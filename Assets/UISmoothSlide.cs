using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UISmoothSlide : MonoBehaviour
{
    // public GameObject target;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // Quaternion oldRotation = this.transform.rotation;
        // Quaternion oldRotation = this.transform.localRotation;
        // Quaternion.Lerp();

        // reset rotation Z
        // this.transform.rotation = new Quaternion(oldRotation.x, oldRotation.y, 0.0f, oldRotation.w);
    }
}
